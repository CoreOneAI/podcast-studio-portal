[build]
  # FINAL FIX: This runs the build and forces a successful exit code (0), 
  # preventing Netlify from deleting the 'dist' folder.
  command = "npm run build && echo SUCCESS"
  publish = "dist"